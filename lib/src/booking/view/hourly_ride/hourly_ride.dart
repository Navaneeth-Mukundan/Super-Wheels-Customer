import 'package:flutter/material.dart';

class HourlyRideScreen extends StatelessWidget {
  const HourlyRideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Hourly Screen')));
  }
}
